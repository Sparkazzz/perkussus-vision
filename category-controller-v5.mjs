const $=(s,r=document)=>r.querySelector(s);
const $$=(s,r=document)=>[...r.querySelectorAll(s)];
const lang=()=>{try{return localStorage.getItem('pv_language')||'it'}catch{return'it'}};
const sleep=ms=>new Promise(r=>setTimeout(r,ms));

if(!window.__PV_CATEGORY_V4__){
 window.__PV_CATEGORY_V4__=true;
 const text={it:{loading:'Ricerca in corso…',results:'risultati',none:'Nessun risultato in questa zona',zoom:'Avvicinati ancora alla città',off:'Filtro disattivato'},en:{loading:'Searching…',results:'results',none:'No results in this area',zoom:'Zoom further into the city',off:'Filter off'}};
 const tr=()=>text[lang()]||text.en;
 let map=null,active='',rows=[],seq=0,aborter=null,moveTimer=null,styleTimer=null;
 const style=document.createElement('style');
 style.textContent='.pv-category-chip{touch-action:manipulation;-webkit-tap-highlight-color:transparent}.pv-category-chip.pv-busy{position:relative}.pv-category-chip.pv-busy:after{content:"";width:10px;height:10px;border:2px solid rgba(255,255,255,.25);border-top-color:#72e8ff;border-radius:50%;margin-left:3px;animation:pvCatSpin .65s linear infinite}.pv-category-chip.active:not(.pv-busy):after{content:"×";font-size:16px;line-height:1;margin-left:2px;opacity:.8}@keyframes pvCatSpin{to{transform:rotate(360deg)}}';
 document.head.appendChild(style);
 function status(msg,error=false){const e=$('#pvMapSearchStatus');if(!e)return;e.textContent=msg;e.classList.toggle('error',error);e.classList.add('show');clearTimeout(status.t);status.t=setTimeout(()=>e.classList.remove('show'),2600)}
 function label(id){return document.querySelector(`[data-pv-cat="${CSS.escape(id)}"]`)?.textContent?.trim()||id}
 function sync(){$$('[data-pv-cat]').forEach(b=>{const on=b.dataset.pvCat===active;b.classList.toggle('active',on);b.classList.toggle('pv-busy',on&&!!aborter);b.setAttribute('aria-pressed',on?'true':'false')})}
 function remove(){if(!map)return;for(const id of ['pv-poi-labels','pv-poi-points'])try{if(map.getLayer(id))map.removeLayer(id)}catch{}try{if(map.getSource('pv-poi-results'))map.removeSource('pv-poi-results')}catch{}}
 function clear(show=true){active='';seq++;aborter?.abort();aborter=null;rows=[];remove();sync();if(show)status(tr().off)}
 function bbox(){const b=map.getBounds();return{south:b.getSouth(),west:b.getWest(),north:b.getNorth(),east:b.getEast()}}
 function render(list,id){remove();rows=list||[];if(!rows.length)return;const features=rows.map((r,n)=>({type:'Feature',geometry:{type:'Point',coordinates:[r.lon,r.lat]},properties:{key:`${r.type}_${r.id}_${n}`,label:r.name||label(id),kind:r.kind||''}}));try{map.addSource('pv-poi-results',{type:'geojson',data:{type:'FeatureCollection',features}});map.addLayer({id:'pv-poi-points',type:'circle',source:'pv-poi-results',paint:{'circle-radius':['interpolate',['linear'],['zoom'],10,6,14,8,17,10],'circle-color':'#72e8ff','circle-stroke-width':2,'circle-stroke-color':'#07101b','circle-opacity':.98}});map.addLayer({id:'pv-poi-labels',type:'symbol',source:'pv-poi-results',minzoom:11.7,layout:{'text-field':['get','label'],'text-size':11,'text-offset':[0,1.35],'text-anchor':'top','text-max-width':12},paint:{'text-color':'#fff','text-halo-color':'#07101b','text-halo-width':1.5}})}catch(e){console.warn('Perkussus category render',e)}}
 async function fetchCategory(id,box,signal){let last;for(let a=0;a<2;a++){try{const u=new URL('/api/nearby',location.origin);Object.entries({mode:'category',category:id,...box,lang:lang()}).forEach(([k,v])=>u.searchParams.set(k,String(v)));const r=await fetch(u,{headers:{accept:'application/json'},cache:'no-store',signal});if(!r.ok)throw new Error(`POI ${r.status}`);return await r.json()}catch(e){if(e.name==='AbortError')throw e;last=e;if(a===0)await sleep(250)}}throw last}
 async function select(id,user=true){if(!map)return;if(user&&active===id)return clear(true);active=id;seq++;const mine=seq;aborter?.abort();aborter=new AbortController();remove();sync();if((map.getZoom?.()||0)<10){aborter=null;sync();return status(tr().zoom,true)}const b=bbox();if(b.north-b.south>1.8||b.east-b.west>1.8){aborter=null;sync();return status(tr().zoom,true)}if(user)status(`${tr().loading} ${label(id)}`);try{const data=await fetchCategory(id,b,aborter.signal);if(mine!==seq||active!==id)return;render(data.rows||[],id);status(`${data.count??0} ${tr().results} · ${label(id)}`,!(data.rows||[]).length)}catch(e){if(e.name!=='AbortError'&&mine===seq)status(tr().none,true)}finally{if(mine===seq){aborter=null;sync()}}}
 function bind(){let down=null,suppressUntil=0,lastHandled=0;document.addEventListener('pointerdown',e=>{const b=e.target.closest?.('[data-pv-cat]');if(b)down={id:b.dataset.pvCat,x:e.clientX,y:e.clientY,t:Date.now()}},true);document.addEventListener('pointerup',e=>{const b=e.target.closest?.('[data-pv-cat]');if(!b||!down)return;const d=down;down=null;const moved=Math.hypot(e.clientX-d.x,e.clientY-d.y),elapsed=Date.now()-d.t;if(moved>12||elapsed>750){suppressUntil=Date.now()+450;return}e.preventDefault();e.stopImmediatePropagation();lastHandled=Date.now();suppressUntil=Date.now()+800;select(b.dataset.pvCat,true)},true);document.addEventListener('click',e=>{const b=e.target.closest?.('[data-pv-cat]');if(!b)return;e.preventDefault();e.stopImmediatePropagation();if(Date.now()<suppressUntil||Date.now()-lastHandled<800)return;select(b.dataset.pvCat,true)},true);const strip=$('#pvCategoryStrip');if(strip)new MutationObserver(sync).observe(strip,{childList:true,subtree:true});sync()}
 async function init(){for(let i=0;i<160;i++){if(window.__PV_MAP__?.getZoom&&$('#pvCategoryStrip')){map=window.__PV_MAP__;break}await sleep(50)}if(!map)return;bind();map.on('moveend',()=>{clearTimeout(moveTimer);if(active)moveTimer=setTimeout(()=>select(active,false),520)});map.on('styledata',()=>{clearTimeout(styleTimer);styleTimer=setTimeout(()=>{if(active&&rows.length&&!map.getSource('pv-poi-results'))render(rows,active)},180)})}
 init();
}
